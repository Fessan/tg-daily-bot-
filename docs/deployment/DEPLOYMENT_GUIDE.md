# 🚀 Полное руководство по развертыванию Telegram Daily Bot

Это объединенное руководство содержит всю информацию о развертывании проекта с использованием Docker и Ansible.

---

## 📖 Содержание

1. [Быстрый старт (5 минут)](#быстрый-старт-5-минут)
2. [Подробное руководство](#подробное-руководство)
3. [Что было добавлено](#что-было-добавлено)
4. [Итоговая сводка](#итоговая-сводка)

---

# Быстрый старт (5 минут)

## 🚀 За 5 минут на боевой сервер

### 1. Подготовка (на вашем компьютере)

```bash
# Установите Ansible
sudo apt install ansible  # Ubuntu/Debian
# или
brew install ansible       # macOS

# Перейдите в директорию проекта
cd tg-daily-bot

# Установите Ansible зависимости
cd deployment/ansible
ansible-galaxy collection install -r requirements.yml
```

### 2. Настройка сервера

Отредактируйте `deployment/ansible/inventory.ini`:

```ini
[production]
prod-server ansible_host=YOUR_SERVER_IP ansible_user=deploy
```

Замените `YOUR_SERVER_IP` на IP вашего сервера.

### 3. Настройка токена бота

Создайте зашифрованный файл с токеном:

```bash
ansible-vault create group_vars/vault.yml
```

Введите пароль и добавьте:

```yaml
vault_bot_token: "YOUR_BOT_TOKEN_FROM_BOTFATHER"
```

### 4. Проверка подключения

```bash
ansible all -i inventory.ini -m ping
```

Должны увидеть `SUCCESS`.

### 5. Деплой! 🎉

```bash
ansible-playbook -i inventory.ini deploy.yml --ask-vault-pass
```

Введите пароль vault и ждите завершения.

### 6. Проверка

```bash
# Просмотр логов
ansible-playbook -i inventory.ini logs.yml

# Или на сервере напрямую
ssh deploy@YOUR_SERVER_IP
docker logs tg-daily-bot
```

## Управление ботом

```bash
# Перезапуск
ansible-playbook -i inventory.ini restart.yml

# Остановка
ansible-playbook -i inventory.ini stop.yml

# Запуск
ansible-playbook -i inventory.ini start.yml

# Бэкап
ansible-playbook -i inventory.ini backup.yml
```

## Использование Makefile (еще проще!)

```bash
# Проверка подключения
make ansible-ping

# Деплой
make deploy

# Перезапуск
make deploy-restart

# Логи
make deploy-logs

# Бэкап
make deploy-backup
```

**Важно**: Убедитесь, что на сервере:
- ✅ Есть пользователь `deploy` с правами sudo
- ✅ Настроен SSH доступ по ключу
- ✅ Открыт порт 22 для SSH

---

# Подробное руководство

Полная документация находится в файле [DEPLOYMENT.md](../../DEPLOYMENT.md) в корне проекта.

## Основные разделы:

1. **Общая информация** - что такое Docker и Ansible деплой
2. **Требования** - что нужно для развертывания
3. **Подготовка локальной машины** - установка Ansible и зависимостей
4. **Подготовка удаленного сервера** - настройка сервера
5. **Настройка Ansible** - конфигурация inventory и переменных
6. **Деплой приложения** - пошаговый процесс
7. **Управление ботом** - команды для управления
8. **Мониторинг и логи** - как следить за работой
9. **Бэкапы** - создание и восстановление
10. **Решение проблем** - troubleshooting

См. полную документацию: [DEPLOYMENT.md](../../DEPLOYMENT.md)

---

# Что было добавлено

## Дата: 2025-11-26

### 🎉 Новые возможности

Проект теперь готов к профессиональному деплою на удаленный сервер с использованием Docker и Ansible!

### 📦 Добавленные компоненты

#### 1. Docker контейнеризация

- **`Dockerfile`** - оптимизированный образ
  - Python 3.12-slim
  - Непривилегированный пользователь
  - Healthcheck для мониторинга
  - Volumes для данных

- **`docker-compose.yml`** - оркестрация контейнера
  - Автоматический перезапуск
  - Монтирование volumes
  - Настройки логирования

- **`.dockerignore`** - оптимизация сборки

#### 2. Ansible автоматизация

Структура `deployment/ansible/`:
- `deploy.yml` - основной playbook
- `start.yml`, `stop.yml`, `restart.yml` - управление
- `logs.yml` - просмотр логов
- `backup.yml` - создание бэкапов
- `inventory.ini` - список серверов
- `group_vars/` - переменные и секреты

**Возможности:**
- ✅ Автоматическая установка Docker
- ✅ Создание пользователя и директорий
- ✅ Автоматические бэкапы
- ✅ Ansible Vault для секретов
- ✅ Healthcheck и проверка состояния

#### 3. Вспомогательные файлы

- **`Makefile`** - удобные команды
- **`env.example`** - шаблон конфигурации
- **Скрипты** - для локального тестирования

### 🔧 Изменения в коде

**config.py** - добавлена поддержка переменных окружения:
```python
DB_PATH = os.getenv("DB_PATH", "bot.db")
LOG_FILE = os.getenv("LOG_FILE", "bot.log")
TIMEZONE = os.getenv("TIMEZONE", "Europe/Moscow")
LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")
```

### ✅ Обратная совместимость

- ✅ Бот работает локально как раньше
- ✅ Существующие базы данных совместимы
- ✅ Не требуется миграция данных

---

# Итоговая сводка

## ✅ Достижения

### 1️⃣ Docker контейнеризация
- Изолированный контейнер
- Volumes для данных и логов
- Healthcheck для мониторинга
- Непривилегированный пользователь

### 2️⃣ Ansible автоматизация
- Полностью автоматизированный деплой
- Управление ботом (start, stop, restart)
- Автоматические бэкапы
- Ansible Vault для секретов

### 3️⃣ Документация
- Полное руководство
- Быстрый старт
- Makefile с командами

### 4️⃣ Обратная совместимость
- Все изменения не ломают функционал
- Локальный запуск работает как раньше

## 📁 Структура проекта

```
tg-daily-bot/
├── 🐳 Docker
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── .dockerignore
│
├── 🤖 Ansible
│   └── deployment/ansible/
│       ├── deploy.yml
│       ├── start/stop/restart.yml
│       ├── logs.yml
│       ├── backup.yml
│       ├── inventory.ini
│       └── group_vars/
│
├── 📖 Документация
│   ├── docs/deployment/
│   ├── DEPLOYMENT.md
│   └── README.md
│
└── 🐍 Исходный код
    ├── main.py
    ├── handlers/
    └── tests/
```

## 🚀 Как начать

### Локальный тест Docker:
```bash
./docker-test.sh
```

### Деплой на сервер:
```bash
make deploy
```

## 🎮 Основные команды

```bash
# Деплой
make deploy              # Полный деплой
make deploy-restart      # Перезапуск
make deploy-logs         # Логи
make deploy-backup       # Бэкап

# Docker локально
make docker-run          # Запуск
make docker-stop         # Остановка
make docker-logs         # Логи
```

## 📊 Преимущества

| Аспект | Преимущество |
|--------|--------------|
| **Изоляция** | Отдельный Docker контейнер |
| **Автоматизация** | Один playbook для деплоя |
| **Безопасность** | Ansible Vault, непривилегированный пользователь |
| **Бэкапы** | Автоматические перед обновлением |
| **Простота** | Одна команда для управления |

## 🔐 Безопасность

✅ Реализовано:
1. Ansible Vault для секретов
2. Непривилегированный пользователь в Docker
3. SSH ключи для доступа
4. Изолированная сеть Docker
5. .env в .gitignore

## 🆘 Помощь

- 📖 Полная документация: [DEPLOYMENT.md](../../DEPLOYMENT.md)
- 🤖 Ansible: [deployment/ansible/README.md](../../deployment/ansible/README.md)
- 💬 Автор: [@Fessan](https://t.me/Fessan)

---

**Проект полностью готов к production деплою! 🎉**

*Версия: 1.0*  
*Дата: 2025-11-26*  
*Автор: @Fessan*



