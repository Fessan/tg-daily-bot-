# Changelog - Добавление Docker и Ansible деплоя

## Дата: 2025-11-26

### 🎉 Новые возможности

Проект теперь готов к профессиональному деплою на удаленный сервер с использованием Docker и Ansible!

### 📦 Добавленные файлы

#### Docker конфигурация

- **`Dockerfile`** - Multi-stage образ с оптимизацией размера и безопасностью
  - Использует Python 3.12-slim
  - Непривилегированный пользователь botuser
  - Healthcheck для мониторинга
  - Volumes для данных и логов

- **`docker-compose.yml`** - Оркестрация контейнера
  - Автоматический перезапуск
  - Монтирование volumes для данных и логов
  - Настройки логирования
  - Ограничения ресурсов (опционально)

- **`.dockerignore`** - Оптимизация сборки образа
  - Исключение виртуальных окружений
  - Исключение тестов и документации
  - Исключение git и IDE файлов

- **`docker-test.sh`** - Скрипт для быстрого тестирования локально

#### Ansible автоматизация

**Структура `deployment/ansible/`:**

```
deployment/ansible/
├── ansible.cfg              # Конфигурация Ansible
├── inventory.ini            # Список серверов
├── requirements.yml         # Необходимые коллекции
├── deploy.yml              # ⭐ Основной playbook для деплоя
├── start.yml               # Запуск бота
├── stop.yml                # Остановка бота
├── restart.yml             # Перезапуск бота
├── logs.yml                # Просмотр логов
├── backup.yml              # Создание бэкапа
├── group_vars/
│   ├── all.yml             # Общие переменные
│   └── production.yml      # Production переменные
├── templates/
│   └── env.j2              # Шаблон .env файла
└── README.md               # Документация Ansible
```

**Возможности Ansible playbook:**

- ✅ Автоматическая установка Docker на сервере
- ✅ Создание пользователя для деплоя
- ✅ Копирование кода проекта
- ✅ Управление конфигурацией через .env
- ✅ Автоматическое создание бэкапов перед обновлением
- ✅ Healthcheck и проверка состояния
- ✅ Очистка старых бэкапов
- ✅ Поддержка Ansible Vault для секретов

#### Документация

- **`DEPLOYMENT.md`** - Полное руководство по деплою (150+ строк)
  - Требования и подготовка
  - Настройка локальной машины
  - Настройка удаленного сервера
  - Пошаговые инструкции
  - Управление ботом
  - Мониторинг и логи
  - Бэкапы
  - Решение проблем
  - Полезные команды

- **`QUICKSTART_DEPLOY.md`** - Быстрый старт (за 5 минут)

- **`deployment/ansible/README.md`** - Документация по Ansible

- **`Makefile`** - Удобные команды для всех операций
  - Локальная разработка (install, run, test)
  - Docker (build, run, stop, logs)
  - Ansible деплой (deploy, start, stop, restart)
  - Бэкапы (backup-local, restore-local)
  - Информация (info, help)

- **`env.example`** - Пример файла с переменными окружения

#### Вспомогательные файлы

- **`data/.gitkeep`** - Директория для базы данных
- **`logs/.gitkeep`** - Директория для логов

### 🔧 Изменения в существующих файлах

#### `config.py`

Добавлена поддержка переменных окружения для Docker:

```python
# Раньше:
DB_PATH = "bot.db"
LOG_FILE = "bot.log"

# Теперь:
DB_PATH = os.getenv("DB_PATH", "bot.db")
LOG_FILE = os.getenv("LOG_FILE", "bot.log")
TIMEZONE = os.getenv("TIMEZONE", "Europe/Moscow")
LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")
```

Это позволяет легко переопределять пути в Docker контейнере через .env файл.

#### `.gitignore`

Расширен для исключения:

- Docker файлов (`docker-compose.override.yml`)
- Ansible секретов (`vault.yml`, `.vault_pass`)
- Директорий с данными (`data/`, `logs/`, `backups/`)
- Временных файлов Ansible (`*.retry`, `ansible.log`)

### 🎯 Преимущества нового деплоя

#### Изоляция
- ✅ Бот работает в отдельном Docker контейнере
- ✅ Не конфликтует с другими проектами на сервере
- ✅ Собственная сеть и ресурсы

#### Безопасность
- ✅ Непривилегированный пользователь в контейнере
- ✅ Ansible Vault для хранения секретов
- ✅ SSH ключи для доступа к серверу
- ✅ Изолированная сеть Docker

#### Удобство
- ✅ Один playbook для деплоя на любой сервер
- ✅ Автоматизация всех операций
- ✅ Простые команды через Makefile
- ✅ Быстрый откат при проблемах

#### Надежность
- ✅ Автоматические бэкапы перед обновлением
- ✅ Healthcheck контейнера
- ✅ Автоматический перезапуск при падении
- ✅ Сохранение данных между обновлениями

#### Масштабируемость
- ✅ Легко добавить новые серверы в inventory
- ✅ Поддержка staging/production окружений
- ✅ Ограничение ресурсов контейнера
- ✅ Мониторинг через Docker metrics

### 📚 Как начать использовать

#### Для локального тестирования Docker:

```bash
# 1. Создайте .env файл
cp env.example .env
# Отредактируйте .env и добавьте ваш BOT_TOKEN

# 2. Запустите тестовый скрипт
./docker-test.sh

# Или используйте Makefile
make docker-run
```

#### Для деплоя на сервер:

```bash
# 1. Настройте Ansible
cd deployment/ansible
nano inventory.ini  # Укажите IP вашего сервера

# 2. Создайте vault с токеном
ansible-vault create group_vars/vault.yml
# Добавьте: vault_bot_token: "YOUR_TOKEN"

# 3. Деплой!
make deploy
# или
ansible-playbook -i inventory.ini deploy.yml --ask-vault-pass
```

Подробные инструкции см. в [DEPLOYMENT.md](DEPLOYMENT.md)

### 🔄 Обратная совместимость

Все изменения полностью обратно совместимы:

- ✅ Бот работает локально как раньше (`python main.py`)
- ✅ Существующие базы данных совместимы
- ✅ Не требуется миграция данных
- ✅ Все команды бота работают как прежде

### 🛠 Системные требования

#### Для Docker деплоя:
- Docker Engine 20.10+
- Docker Compose v2
- 1GB RAM минимум
- 10GB дискового пространства

#### Для Ansible деплоя:
- Ansible 2.10+
- Python 3.8+
- SSH доступ к серверу
- Ubuntu 20.04+ / Debian 11+ на сервере

### 📞 Поддержка

- 📖 Полная документация: [DEPLOYMENT.md](DEPLOYMENT.md)
- 🚀 Быстрый старт: [QUICKSTART_DEPLOY.md](QUICKSTART_DEPLOY.md)
- 🤖 Ansible: [deployment/ansible/README.md](deployment/ansible/README.md)
- 💬 Автор: [@Fessan](https://t.me/Fessan)

### 🎊 Заключение

Проект теперь готов к профессиональному production деплою! Все процессы автоматизированы, документированы и протестированы. 

Приятного использования! 🚀

